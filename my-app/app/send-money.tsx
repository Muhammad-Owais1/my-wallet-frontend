import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const sendMoney = () => {
  return (
    <SafeAreaView className="flex-1 bg-slate-900 py-8 px-4 gap-12"></SafeAreaView>
  );
};

export default sendMoney;
